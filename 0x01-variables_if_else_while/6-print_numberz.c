#include <stdio.h>

/**
 * main - Entry point of the program
 * Return: Returns 0 when successful
 */
int main(void)
{
	int numb = '48';

	while (numb <= '57')
	{
		putchar(numb);
		numb++;
	}

	putchar('\n');
	return (0);

}
