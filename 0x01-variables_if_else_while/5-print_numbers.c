#include <stdio.h>

/**
 * main - Entry point of the program
 * Return: Returns 0 when successful
 */
int main(void)
{
	int numb = 0;

	while (numb < 10)
	{
		printf("%d\n", numb);
		numb++;
	}

	return (0);

}
